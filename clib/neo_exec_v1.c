#include "neo_exec.h"

void neo_exec_v1_init(int slot) {
    // neo_printf("%d\n", slot);
    struct neo_var_v1 *v;
    v = &(neo_slot[slot].var.v1);
}

void neo_exec_v1_draw(int slot) {

}
